DEEPTUTOR_PORT=3782
TZ=Asia/Shanghai

# DeepTutor model settings are configured in the DeepTutor Web UI.
# Recommended custom OpenAI-compatible provider when your existing gateway
# exposes the API on the same host:
# Base URL: http://host.docker.internal:3080/v1
# Model: deepseek-chat
# API Key: use the token required by your gateway; do not commit real keys here.
